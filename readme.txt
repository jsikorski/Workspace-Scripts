Installation:
1. Ensure Git is installed.
2. Ensure Pageant is installed (if you want to use it).
3. Ensure you have RemoteSigned or Unrestricted ExecutionPolicy 
   (Get-ExecutionPolicy PowerShell command).
4. Set correct settings in configuration.ps1 file.
5. Run install.ps1.